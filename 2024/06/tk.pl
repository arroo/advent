#!/usr/bin/env perl

use warnings;
use strict;

use Tk;
use Tk::Xlib;

use AOC::Base qw(:all);
use AOC::Math qw(:all);
use AOC::Utils qw(:all);

use Data::Dumper;
$Data::Dumper::Sortkeys = 1;

sub solveOne {
	my ($lines) = @_;

	return -1;
}

sub solveTwo {
	my ($lines) = @_;

	return -1;
}

main(\&solveOne, \&solveTwo);
